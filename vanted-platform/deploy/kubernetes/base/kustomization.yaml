apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vanted

resources:
  - namespace.yaml
  - configmap.yaml

# Add service Deployments/Services/HPA/PDB/NetworkPolicy resources here as each
# microservice reaches production-ready parity. Images are supplied by CI/CD.
